## 🧪 Automated Tests for Game Development

Этот проект демонстрирует навыки автоматизации тестирования в разработке игр на Unity. Он включает полный набор unit- и integration-тестов, а также CI/CD пайплайн для автоматического запуска тестов.

### 🎯 Особенности проекта
- **Unit-тесты** для игровой логики (GameManager, Coin)
- **Integration-тесты** (PlayMode) с имитацией действий игрока
- **CI/CD конфигурация** с GitHub Actions
- **Поддержка разных типов тестов**: EditMode и PlayMode
- **Чистая архитектура тестов** с использованием NUnit и Unity Test Framework

### 📁 Структура тестов
- **EditMode Tests**: Юнит-тесты для изолированной проверки игровой логики (например, расчёт очков в GameManager)
- **PlayMode Tests**: Интеграционные тесты, которые запускают сцену и симулируют взаимодействие игрока с объектами

### 🛠️ Технологии, использованные для тестирования
- **Unity Test Framework (UTF)** с NUnit
- **GitHub Actions** для Continuous Integration
- **Unity Input System** для тестирования управления игроком
- **TMPro** для UI-элементов (отображение счёта)

### 📦 Зависимости
Для запуска проекта убедитесь, что установлены:
- Unity 2022.3 LTS или выше
- Пакеты через Package Manager:
  - `Unity.Test-Framework` (версия 1.1.33+)
  - `Input System` (версия 1.7.0+)
  - `TextMeshPro` (входит в стандартные ассеты Unity)

### 🗂️ Структура проекта
Ключевые файлы:
- `GameManager.cs` – управление игровым состоянием, счётом и условием победы
- `Coin.cs` – логика монеты, добавляет очки при сборе
- `PlayerController.cs` – управление персонажем через Input System
- `GameManagerTests.cs` – юнит-тесты для GameManager (EditMode)
- `GameplayIntegrationTests.cs` – интеграционные тесты игрового процесса (PlayMode)
- `run-tests.yaml` – конфигурация GitHub Actions для CI

### 🚀 Как запустить тесты

#### Способ 1: Через Unity Test Runner
1. Откройте проект в Unity
2. Перейдите в `Window → General → Test Runner`
3. Выберите вкладку `EditMode` или `PlayMode`
4. Нажмите `Run All` или выберите конкретные тесты

#### Способ 2: Через командную строку (для CI)
```bash
# Для EditMode тестов
Unity -runTests -batchmode -testPlatform editmode -projectPath .
# Для PlayMode тестов
Unity -runTests -batchmode -testPlatform playmode -projectPath .
```

#### Способ 3: Автоматически через GitHub Actions
Тесты запускаются автоматически при каждом push в ветку `main` или при создании pull request. Конфигурация находится в `.github/workflows/run-tests.yaml`.

### ✅ Статус CI/CD
![CI Status](https://github.com/piligrim-curious/SimpleGame-AutomatedTests/actions/workflows/run-tests.yml/badge.svg)

### 📊 Результаты тестирования
- **EditMode тесты** проверяют корректность игровой логики:
  - Инициализация счёта
  - Добавление очков
  - Условие победы
  - Сброс игры
- **PlayMode тесты** проверяют интеграцию:
  - Сбор монет игроком
  - Уничтожение монет после сбора
  - Активация условия победы

### 🤝 Вклад в проект
Если вы хотите предложить улучшения:
1. Форкните репозиторий
2. Создайте ветку для своей функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### 📄 Лицензия
Этот проект распространяется под лицензией MIT. См. файл [`LICENSE`](LICENSE) в корне проекта для подробностей.

**Кратко о MIT-лицензии:**
- ✅ Можно свободно использовать, копировать, модифицировать, распространять
- ✅ Можно использовать в коммерческих проектах
- ✅ Обязательно указывать оригинальное авторство
- ⚠️ Автор не несёт ответственности за возможные проблемы

*Если вы планируете использовать этот проект в своих работах, пожалуйста, укажите оригинального автора.*

---

*Проект создан для демонстрации навыков автоматизации тестирования в игровой разработке. Все компоненты готовы к интеграции в реальный проект Unity.*